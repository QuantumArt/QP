@using Quantumart.QP8.Resources
@model Quantumart.QP8.WebMvc.ViewModels.Abstract.LockableEntityViewModel

@if (!Model.IsNew)
{
	using(Html.BeginFieldGroup(GlobalStrings.LockingGroup))
	{
		@Html.DisplayFieldFor(s => s.LockableData.LockedByDisplayName)
		@Html.DisplayFieldFor(s => s.LockableData.LockedToDisplay)

		if (Model.LockableData.LockedByYou)
		{     
			@Html.CheckBoxFieldFor(s => s.LockableData.PermanentLock)
		}

		if (Model.LockableData.CanBeUnlocked)
        { 
            @Html.UnlockLink(Model)
        }
    }
}
